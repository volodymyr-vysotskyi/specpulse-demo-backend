{
  "name": "specpulse-demo-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
